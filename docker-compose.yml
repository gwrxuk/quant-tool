services:
  quant-tool:
    build: .
    container_name: quant-options-toolkit
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - STREAMLIT_SERVER_RUN_ON_SAVE=true
    restart: unless-stopped
